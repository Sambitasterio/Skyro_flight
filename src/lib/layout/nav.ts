/**
 * Transparent navbar over the hero (white text) when at top of home page.
 * Enabled with dark hero shell (Phase 3.3 adds rotating destination photos).
 */
export const HERO_TRANSPARENT_NAV = true;
