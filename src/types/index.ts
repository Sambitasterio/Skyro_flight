/** Domain types used across stores and UI. */

export type { ActiveBooking } from "@/types/booking";
export type { BookingRow as Booking, FlightRow as Flight, SeatRow as Seat } from "@/types/database";
export type {
  BookingStep,
  CabinClass,
  SearchQuery,
  SelectedFlight,
  SelectedSeat,
  TripType,
} from "@/types/flight";
export type { PassengerFormData } from "@/types/passenger";
