# Skyro — Design assets

Used at **Phase 3** (landing page). The agent copies JPGs from `destinations/` to `public/hero/` when building the UI.

| Folder | Files | Use |
|--------|-------|-----|
| [`references/`](./references/) | `skyscanner*.png`, `easemytrip*.png`, `ixigo*.png` | Frontpage layout inspiration (dev reference only) |
| [`destinations/`](./destinations/) | 6× Unsplash JPGs | Hero rotation + trending destination cards |

Do **not** use PNG screenshots as hero backgrounds — JPGs only in production UI.
